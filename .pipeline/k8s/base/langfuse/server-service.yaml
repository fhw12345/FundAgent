apiVersion: v1
kind: Service
metadata:
  name: langfuse-server
  labels:
    app: langfuse-server
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: langfuse-server
