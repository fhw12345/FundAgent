apiVersion: apps/v1
kind: Deployment
metadata:
  name: langfuse-server
  labels:
    app: langfuse-server
    component: observability
spec:
  replicas: 1
  selector:
    matchLabels:
      app: langfuse-server
  template:
    metadata:
      labels:
        app: langfuse-server
        component: observability
    spec:
      serviceAccountName: klinematrix-sa
      imagePullSecrets:
      - name: acr-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: langfuse
        image: financialagent-gxftdbbre4gtegea.azurecr.io/klinecubic/langfuse-server:3
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
          name: http
        env:
        # Database - PostgreSQL (in-cluster)
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: langfuse-secrets
              key: postgres-password
        - name: DATABASE_URL
          value: "postgresql://langfuse:$(POSTGRES_PASSWORD)@langfuse-postgres:5432/langfuse"

        # ClickHouse - Define password first, then use in MIGRATION_URL
        - name: CLICKHOUSE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: langfuse-secrets
              key: clickhouse-password
        - name: CLICKHOUSE_URL
          value: "http://langfuse-clickhouse:8123"
        - name: CLICKHOUSE_USER
          value: "langfuse"
        - name: CLICKHOUSE_HOST
          value: "langfuse-clickhouse"
        - name: CLICKHOUSE_DATABASE
          value: "langfuse"
        - name: CLICKHOUSE_MIGRATION_URL
          value: "clickhouse://langfuse:$(CLICKHOUSE_PASSWORD)@langfuse-clickhouse:9000/langfuse"
        - name: CLICKHOUSE_CLUSTER_ENABLED
          value: "false"

        # Redis
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"

        # S3/OSS Event Uploads
        # SECURITY NOTE: Using HTTP instead of HTTPS due to boto3 chunked encoding incompatibility with OSS
        # Alibaba Cloud OSS does not support AWS SDK's aws-chunked encoding when using HTTPS
        # Traces are primarily stored in ClickHouse (encrypted at rest); OSS is backup storage
        - name: LANGFUSE_S3_EVENT_UPLOAD_BUCKET
          value: "klinecubic-financialagent-oss"
        - name: LANGFUSE_S3_EVENT_UPLOAD_PREFIX
          value: "langfuse-events"
        - name: LANGFUSE_S3_EVENT_UPLOAD_REGION
          value: "cn-shanghai"
        - name: LANGFUSE_S3_EVENT_UPLOAD_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: langfuse-secrets
              key: oss-access-key-id
        - name: LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: langfuse-secrets
              key: oss-access-key-secret
        - name: LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT
          value: "http://oss-cn-shanghai.aliyuncs.com"  # HTTP required (boto3 chunked encoding issue)
        - name: LANGFUSE_S3_EVENT_UPLOAD_FORCE_PATH_STYLE
          value: "false"

        # Auth
        - name: NEXTAUTH_URL
          value: "https://monitor.klinecubic.cn"
        - name: NEXTAUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: langfuse-secrets
              key: nextauth-secret
        - name: SALT
          valueFrom:
            secretKeyRef:
              name: langfuse-secrets
              key: salt

        # Config
        - name: TELEMETRY_ENABLED
          value: "false"
        - name: LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES
          value: "true"

        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
              - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: nextjs-cache
          mountPath: /app/.next/cache
        resources:
          requests:
            memory: "384Mi"
            cpu: "100m"
          limits:
            memory: "768Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/public/health
            port: 3000
          initialDelaySeconds: 120
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/public/health
            port: 3000
          initialDelaySeconds: 60
          periodSeconds: 15
          timeoutSeconds: 10
      volumes:
      - name: tmp
        emptyDir: {}
      - name: nextjs-cache
        emptyDir: {}
