apiVersion: v1
kind: Service
metadata:
  name: langfuse-postgres
  labels:
    app: langfuse-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: langfuse-postgres
