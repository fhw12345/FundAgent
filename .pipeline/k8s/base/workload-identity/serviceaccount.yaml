apiVersion: v1
kind: ServiceAccount
metadata:
  name: financial-agent-sa
  namespace: financial-agent-dev
  labels:
    app.kubernetes.io/name: financial-agent
  annotations:
    azure.workload.identity/client-id: "${AZURE_CLIENT_ID}"
    azure.workload.identity/tenant-id: "${AZURE_TENANT_ID}"