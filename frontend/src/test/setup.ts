/**
 * Vitest setup file
 * Runs before all tests
 */

// Basic setup - extend as needed when @testing-library/react is added
// import { cleanup } from "@testing-library/react";
// import { afterEach } from "vitest";

// afterEach(() => {
//   cleanup();
// });

// For now, just export empty config
export {};
